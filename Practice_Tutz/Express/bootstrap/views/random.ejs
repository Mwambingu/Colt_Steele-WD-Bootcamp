<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Random</title>
    </head>
    <body>
        <h1>Your random number is: <%= randNum %></h1>
        <% if(randNum %2 === 0) { %>
        <p>That number is even!</p>
        <% } else {%>
        <p>That number is odd!</p>
        <% }%>

        <h1>Listing Random Numbers:</h1>
        <%let i = 0; %> <%while(i != 10) { %>
        <h1><%= Math.floor(Math.random() * 100 + 1) %></h1>
        <% i++; %> <% } %>
    </body>
</html>
